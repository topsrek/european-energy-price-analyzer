# Session Log - 2026-05-04

## Scope

Prepared the old Austrian electricity price analysis repo for the new EEPA direction:

- Project/product name: European Energy Price Analyzer (EEPA)
- First country route: Austria, `EEPA-AT`
- Public domain: `https://eepa.topsrek.top`
- NPM-facing backend target: `http://192.168.1.21:49173`
- Deployment platform: Coolify on `coolify1`
- GitHub repo: `topsrek/european-energy-price-analyzer`

## Repository Changes

- Renamed the project from the old Austrian-only naming to EEPA.
- Renamed local repo folder to:
  - `/home/topsrek/src/european-energy-price-analyzer`
- Renamed GitHub repo and updated `origin`:
  - `git@github.com:topsrek/european-energy-price-analyzer.git`
- Removed old Lovable references and mock/generated data behavior.
- Kept the recovered real Austrian binary data and pipeline artifacts.
- Added tests around the real binary decoder artifact.
- Added Playwright E2E coverage for:
  - Austria page loading the committed real dataset.
  - Stored country preference redirecting `/` to `/at`.
- Added dev-server `/api/*` file serving so local E2E matches production URL shape.

## Data And API

- Real Austria hourly binary data is served from:
  - `/api/at_electricity_prices.bin`
- Worker/API endpoints:
  - `/api/health`
  - `/api/geoip`
  - `/api/data-manifest`
  - `/api/at_electricity_prices.bin`
- The worker still also accepts the non-`/api` paths internally for direct access.
- GeoIP detection defaults to same-origin `/api/geoip`.
- Country selection is stored in localStorage under:
  - `eepa.selectedRegion`

## Deployment Architecture

Final deployed setup after clarification:

- NPM forwards `eepa.topsrek.top` to:
  - `http://192.168.1.21:49173`
- The `49173` Coolify service serves:
  - the built frontend SPA (`/`, `/at`, static assets)
  - the worker/API/data routes (`/api/*`)
  - the daily dataset updater
- SSL is handled by NPM/OpenResty in front.
- Coolify app:
  - name: `eepa-app`
  - uuid: `nmu27zox7uqup1wngzgf0ie9`
  - port expose: `49173`
  - host port mapping: `49173:49173`
  - no public Coolify subdomain
- Former Coolify web app:
  - name: `eepa-web`
  - uuid: `h8vuuw2fxg7ws7eqejq4pnny`
  - deleted on 2026-06-03 because it duplicated the same app container without serving the public NPM target.

## Verified Live URLs

Verified after final deployment:

- `https://eepa.topsrek.top/` returns `200` and EEPA HTML.
- `https://eepa.topsrek.top/at` returns `200`.
- `https://eepa.topsrek.top/api/health` returns `200`.
- `https://eepa.topsrek.top/api/at_electricity_prices.bin` returns `200`.
- `https://eepa.topsrek.top/api/at_electricity_prices.bin` content length was `133446` bytes.
- Direct LAN target works:
  - `http://192.168.1.21:49173/`
  - `http://192.168.1.21:49173/api/at_electricity_prices.bin`

## Verification Commands Run

Local checks run successfully:

- `npm run typecheck`
- `npm run test`
- `python3 -m py_compile worker/server.py scripts/*.py`
- `npm run build`
- `npm run test:e2e`
- `sudo -n docker build -f worker/Dockerfile -t eepa-worker-local:49173-smoke .`
- `npm run lint`

Lint currently passes with 9 existing warnings, mostly React Fast Refresh export warnings and one hook dependency warning.

## Pushed Commits

Relevant pushed commits from this session:

- `ed57841` Add binary decoder artifact tests
- `ddd5dce` Prepare EEPA worker and deployment tests
- `b7afef2` Configure EEPA deployment services
- `19e1621` Install curl for worker health checks
- `888ec94` Route EEPA data API through web origin
- `a380c6a` Serve EEPA app from worker port

## Notes

- Coolify API access uses `~/.secrets/coolify.env`.
- Do not `source` that file because the token contains `|`; parse values with `grep`/`cut`.
- Coolify deployments were forced via the API after each pushed deployment commit.
- Final NPM-facing port is `49173`, not `80`.
- Future country expansion should keep country data lazy-loaded by route/selection.
